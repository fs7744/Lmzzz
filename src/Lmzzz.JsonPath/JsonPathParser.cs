using Lmzzz.Chars;
using Lmzzz.Chars.Fluent;
using static Lmzzz.Chars.Fluent.Parsers;

namespace Lmzzz.JsonPath;

public class JsonPathParser
{
    //public Parser<IStatement> JsonPath_Query = RootIdentifier.And(Segments);
    //public Parser<IStatement> Segments = ZeroOrMany(S.And(segment));
    //public Parser<IStatement> B = Char();
    //public Parser<char> RootIdentifier = Char('$');
}